[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "artifex-dev"
version = "0.1.0"
description = "Agent-neutral development control, continuity, and understanding layer"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "Apache-2.0"}
authors = [{name = "ARTIFEX Contributors"}]
dependencies = [
  "jsonschema>=4.23,<5",
  "pyyaml>=6.0.2,<7",
  "typer>=0.16,<1",
]

[project.scripts]
artifex = "artifex.cli:app"

[dependency-groups]
dev = [
  "hypothesis>=6.130,<7",
  "mypy>=1.15,<2",
  "pyinstaller>=6.12,<7",
  "pytest>=8.3,<9",
  "pytest-cov>=6,<7",
  "ruff>=0.11,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/artifex"]

[tool.pytest.ini_options]
addopts = "--strict-markers --strict-config -ra"
testpaths = ["tests"]
markers = [
  "unit: isolated deterministic tests",
  "integration: multi-component tests",
  "architecture: dependency and invariant tests",
  "adversarial: failure and trust-boundary tests",
  "conformance: integration contract tests",
  "packaging: frozen distribution tests",
  "slow: tests excluded from the default fast loop",
]

[tool.coverage.run]
branch = true
source = ["artifex"]

[tool.coverage.report]
fail_under = 85
show_missing = true
exclude_also = [
  '\.\.\.$',
  'if __name__ == .__main__.:',
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["artifex"]

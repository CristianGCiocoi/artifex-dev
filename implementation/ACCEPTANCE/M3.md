# M3 Acceptance Report

## Identity

- Milestone: `M3`
- Canonical M2 base: `e1e4cd5e07aac4b8b380111807259cad7a550c0f`
- Implementation baseline commit: `b7efb7b08fee418715a8f05faa450649b121f6f6`
- Acceptance commit: pending canonicalization
- Contract digest: `4c715b81581f4688f3b38d34829a967760e7f862f8408a477f37245f886d07eb`

## Accepted outcome

M3 delivers persisted secret-free provider setup, a public Capability Graph and
contextual Resolver, independently certified Codex `INTERACTION` and
`EXECUTION_IMPLEMENTER` roles, and a real installed-wheel Codex ProjectJob.

The live ProjectJob executed in a self-contained isolated Git workspace, produced a
hash-bound artifact and durable evidence, remained unaccepted after provider completion,
was accepted by a distinct Acceptance Authority actor, and was promoted by Project
Authority to semantic revision 2. Coordinator fencing remained active through a bounded
heartbeat and emitted explicit semantic audit outcomes.

## Evidence classes

All ten M3 evidence classes are `PASS`. The public outcome used a clean installed wheel,
new CLI processes, Codex CLI 0.150.1 with authenticated native session, no source-tree
product import, no custom Application factory, no provider injection and no simulated
provider.

## Journey boundary

- `J16` is `PASS`: a fresh process consumed persisted setup, registered Codex, and the
  Resolver excluded a healthy but contextually ineligible provider.
- `J01` is proven only as `M3_VERTICAL_SLICE_ONLY`. The full clean-machine installer and
  managed-service journey remains explicitly unclaimed until M7.

## Provider claims

Codex `INTERACTION` and `EXECUTION_IMPLEMENTER` completed all five certification ladder
steps through `LIVE_ROLE_CERTIFIED`. Codex `HARNESS` is not claimed.

## Controlled V1 baseline failure

The reproduced `V1-R01-DASHBOARD-FIXTURE-SCHEMA-DRIFT` regression remains preserved.
M3 does not alter `tests/test_release.py` or claim that the V1 release fixture is repaired.

## Verdict

`ACCEPTED`

M4, M5 and M6A become dependency-ready after canonical M3 integration. Optional M6B
remains unclaimed because no Hermes provider claim was authorized.

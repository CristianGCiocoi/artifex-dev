# M0 Evidence

Machine-readable evidence in this directory is scrubbed, source-bound and referenced by the
acceptance state. Test caches, raw environments and secrets are never implementation authority.

